# SER Refactor TODO

- [x] Inspect `ser/__init__.py` to confirm `SERRegressor` export path.
- [x] Refactor `ser/model.py`:
  - [x] Implement `SERRegressor` as sole SER algorithm owner.
  - [x] Move all algorithmic steps from `run_pipeline()` into `SERRegressor.fit()` in required order.
  - [x] Ensure learned artifacts are stored on `self`:
    - [x] `self.global_model`
    - [x] `self.segmentation`
    - [x] `self.local_experts`
    - [x] `self.group_performance`
    - [x] `self.alpha`
    - [x] `self.summary`
  - [x] Keep `predict()` minimal (global prediction + `blended_predict()` only).
  - [x] Keep `fit_predict()` as `fit` then `predict`.
  - [x] Add properties:
    - [x] `thresholds`
    - [x] `experts`
    - [x] `performances`
  - [x] Remove duplicated logic from `run_pipeline()` and make it a thin compatibility wrapper.
  - [x] Remove import-time dataset/demo execution from module.
- [x] Update `ser/__init__.py` only if needed.
- [x] Update TODO progress after each completed step.
- [ ] Run validation command using `experiments/test1.py`.
